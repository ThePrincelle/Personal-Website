version: '3.5'

services:

  sample-app:
    container_name: cv_princelle
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - '2345:3000'
    environment:
      - NODE_ENV=production
